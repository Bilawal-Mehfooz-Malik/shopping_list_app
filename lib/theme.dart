import 'package:flutter/material.dart';

var lightTheme = ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.blueAccent),
  listTileTheme:
      const ListTileThemeData(tileColor: Color.fromARGB(255, 234, 234, 234)),
);
